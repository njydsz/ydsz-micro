/**
 * ESLint 全局配置。
 * 
 * @remarks
 * 基于 @remi/eslint-config 共享规则集，覆盖 TypeScript/Vue 代码规范。
 * 
 * @author remi-team
 * @since 1.0.0
 */
// @ts-check

import { defineConfig } from '@remi/eslint-config';

export default defineConfig();
