/* eslint-disable */
/** auto-generated by bash/gen-contract.py — DO NOT EDIT */
// @data-file 纯类型 API 封装生成件（不计入行数/复杂度规则）
/**
 * RuntimeController API 封装（auto-generated by bash/gen-contract.py）
 *
 * <p>对应后端 {@code RuntimeController}，共 5 个端点。
 * <p>路径规范: /api/v1/agent/**（kebab-case），成功码统一为 code === 'A00000'。
 *
 * @author ydsz-team
 * @auto-generated 请勿手动修改；后端契约变更后执行 {@code python bash/gen-contract.py agent} 重新生成
 * @since 1.0.0
 */
import { requestClient } from '#/api/request';
import type { RuntimeSession } from './models';

/**
 * getActiveSessions: GET /api/v1/agent/runtime/sessions/active
 */
export function getActiveSessions(params: {
    tenantId?: string;
  }): Promise<RuntimeSession[]> {
  return requestClient.get<RuntimeSession[]>(`/api/v1/agent/runtime/sessions/active`, { params });
}

/**
 * getRecentSessions: GET /api/v1/agent/runtime/sessions/recent
 */
export function getRecentSessions(params: {
    limit?: number;
  }): Promise<RuntimeSession[]> {
  return requestClient.get<RuntimeSession[]>(`/api/v1/agent/runtime/sessions/recent`, { params });
}

/**
 * getSession: GET /api/v1/agent/runtime/sessions/{executionId}
 */
export function getSession({ executionId }: {
    executionId: string;
  }): Promise<RuntimeSession> {
  return requestClient.get<RuntimeSession>(`/api/v1/agent/runtime/sessions/${executionId}`);
}

/**
 * getOverview: GET /api/v1/agent/runtime/overview
 */
export function getOverview(): Promise<unknown> {
  return requestClient.get<unknown>(`/api/v1/agent/runtime/overview`);
}

/**
 * forceRecycle: DELETE /api/v1/agent/runtime/sessions/{executionId}
 */
export function forceRecycle({ executionId }: {
    executionId: string;
  }): Promise<boolean> {
  return requestClient.delete<boolean>(`/api/v1/agent/runtime/sessions/${executionId}`);
}
