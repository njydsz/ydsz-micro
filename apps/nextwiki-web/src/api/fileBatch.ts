/** auto-generated by bash/gen-contract.py — DO NOT EDIT */
// @data-file 纯类型 API 封装生成件（不计入行数/复杂度规则）
/**
 * FileBatchController API 封装（auto-generated by bash/gen-contract.py）
 *
 * <p>对应后端 {@code FileBatchController}，共 9 个端点。
 * <p>路径规范: /api/v1/nextwiki/**（kebab-case），成功码统一为 code === 'A00000'。
 *
 * @author ydsz-team
 * @path apps\nextwiki-web\src\api\fileBatch.ts
 * @auto-generated 请勿手动修改；后端契约变更后执行 {@code python bash/gen-contract.py nextwiki} 重新生成
 * @since 1.0.0
 */
import { requestClient } from '#/api/request';
import type { BatchResultDTO, FileNodeVO, FileVersionVO } from './models';

/**
 * batchDelete: POST /api/v1/nextwiki/files/batch/delete
 */
export function batchDelete(data: string[]): Promise<BatchResultDTO> {
  return requestClient.post<BatchResultDTO>(`/api/v1/nextwiki/files/batch/delete`, data);
}

/**
 * batchMove: POST /api/v1/nextwiki/files/batch/move
 */
export function batchMove(data: Record<string, unknown>): Promise<BatchResultDTO> {
  return requestClient.post<BatchResultDTO>(`/api/v1/nextwiki/files/batch/move`, data);
}

/**
 * getVersionHistory: GET /api/v1/nextwiki/files/{nodeId}/versions
 */
export function getVersionHistory({ nodeId }: {
    nodeId: string;
  }): Promise<FileVersionVO[]> {
  return requestClient.get<FileVersionVO[]>(`/api/v1/nextwiki/files/${nodeId}/versions`);
}

/**
 * rollbackVersion: POST /api/v1/nextwiki/files/{nodeId}/versions/{version}/rollback
 */
export function rollbackVersion({ nodeId, version }: {
    nodeId: string;
    version: number;
  }): Promise<FileNodeVO> {
  return requestClient.post<FileNodeVO>(`/api/v1/nextwiki/files/${nodeId}/versions/${version}/rollback`);
}

/**
 * diffVersions: GET /api/v1/nextwiki/files/{nodeId}/versions/diff
 *
 * <p>返回 unknown 的理由（云顶编码规范 §3.1 特殊场景豁免）：
 * 后端方法声明为 {@code YdszResponse}，响应结构未固定为具名 VO，
 * 无法在生成期推导出稳定字段，故不使用 any，退守为 unknown。
 * 调用方应在使用前做类型收窄（参见规范 §3.1 的 isUserInfo 参考实现）。
 */
export function diffVersions({ nodeId }: {
    nodeId: string;
  }, params: {
    oldVersion?: number;
    newVersion?: number;
  }): Promise<unknown> {
  return requestClient.get<unknown>(`/api/v1/nextwiki/files/${nodeId}/versions/diff`, { params });
}

/**
 * asyncBatchDelete: POST /api/v1/nextwiki/files/batch/async-delete
 */
export function asyncBatchDelete(data: string[]): Promise<string> {
  return requestClient.post<string>(`/api/v1/nextwiki/files/batch/async-delete`, data);
}

/**
 * asyncBatchMove: POST /api/v1/nextwiki/files/batch/async-move
 */
export function asyncBatchMove(data: Record<string, unknown>): Promise<string> {
  return requestClient.post<string>(`/api/v1/nextwiki/files/batch/async-move`, data);
}

/**
 * getBatchTaskStatus: GET /api/v1/nextwiki/files/batch/task/{taskId}
 *
 * <p>返回 unknown 的理由（云顶编码规范 §3.1 特殊场景豁免）：
 * 后端方法声明为 {@code YdszResponse}，响应结构未固定为具名 VO，
 * 无法在生成期推导出稳定字段，故不使用 any，退守为 unknown。
 * 调用方应在使用前做类型收窄（参见规范 §3.1 的 isUserInfo 参考实现）。
 */
export function getBatchTaskStatus({ taskId }: {
    taskId: string;
  }): Promise<unknown> {
  return requestClient.get<unknown>(`/api/v1/nextwiki/files/batch/task/${taskId}`);
}

/**
 * toggleStar: PUT /api/v1/nextwiki/files/{nodeId}/star
 */
export function toggleStar({ nodeId }: {
    nodeId: string;
  }): Promise<void> {
  return requestClient.put<void>(`/api/v1/nextwiki/files/${nodeId}/star`);
}
