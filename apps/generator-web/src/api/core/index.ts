/**
 * 代码生成器子应用 / API 模块总入口。
 *
 * <p>统一导出认证（auth）、用户信息（user）、菜单（menu）三个子模块，
 * <p>应用内可按 '#/api/core' 路径一次性导入全部前端接口定义。
 *
 * @path apps/generator-web/src/api/core/index.ts
 */
export * from './auth';
export * from './user';
export * from './menu';
