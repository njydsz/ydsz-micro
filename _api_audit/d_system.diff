diff --git a/apps/system-web/src/api/appInfo.ts b/apps/system-web/src/api/appInfo.ts
index 1e555aa5e..1de276460 100644
--- a/apps/system-web/src/api/appInfo.ts
+++ b/apps/system-web/src/api/appInfo.ts
@@ -4,7 +4,7 @@
  * AppInfoController API 封装（auto-generated by bash/gen-contract.py）
  *
  * <p>对应后端 {@code AppInfoController}，共 5 个端点。
- * <p>路径规范: /api/system/**（kebab-case），成功码统一为 code === 'A00000'。
+ * <p>路径规范: /api/v1/system/**（kebab-case），成功码统一为 code === 'A00000'。
  *
  * @author ydsz-team
  * @auto-generated 请勿手动修改；后端契约变更后执行 {@code python bash/gen-contract.py system} 重新生成
diff --git a/apps/system-web/src/api/auditAdmin.ts b/apps/system-web/src/api/auditAdmin.ts
index 0dc70470e..8d5e3ef9b 100644
--- a/apps/system-web/src/api/auditAdmin.ts
+++ b/apps/system-web/src/api/auditAdmin.ts
@@ -4,7 +4,7 @@
  * AuditAdminController API 封装（auto-generated by bash/gen-contract.py）
  *
  * <p>对应后端 {@code AuditAdminController}，共 5 个端点。
- * <p>路径规范: /api/system/**（kebab-case），成功码统一为 code === 'A00000'。
+ * <p>路径规范: /api/v1/system/**（kebab-case），成功码统一为 code === 'A00000'。
  *
  * @author ydsz-team
  * @auto-generated 请勿手动修改；后端契约变更后执行 {@code python bash/gen-contract.py system} 重新生成
diff --git a/apps/system-web/src/api/config.ts b/apps/system-web/src/api/config.ts
index 2641d3522..6e38d5b08 100644
--- a/apps/system-web/src/api/config.ts
+++ b/apps/system-web/src/api/config.ts
@@ -4,7 +4,7 @@
  * ConfigController API 封装（auto-generated by bash/gen-contract.py）
  *
  * <p>对应后端 {@code ConfigController}，共 12 个端点。
- * <p>路径规范: /api/system/**（kebab-case），成功码统一为 code === 'A00000'。
+ * <p>路径规范: /api/v1/system/**（kebab-case），成功码统一为 code === 'A00000'。
  *
  * @author ydsz-team
  * @auto-generated 请勿手动修改；后端契约变更后执行 {@code python bash/gen-contract.py system} 重新生成
diff --git a/apps/system-web/src/api/configVersion.ts b/apps/system-web/src/api/configVersion.ts
index 1c2304885..005aaa943 100644
--- a/apps/system-web/src/api/configVersion.ts
+++ b/apps/system-web/src/api/configVersion.ts
@@ -4,7 +4,7 @@
  * ConfigVersionController API 封装（auto-generated by bash/gen-contract.py）
  *
  * <p>对应后端 {@code ConfigVersionController}，共 2 个端点。
- * <p>路径规范: /api/system/**（kebab-case），成功码统一为 code === 'A00000'。
+ * <p>路径规范: /api/v1/system/**（kebab-case），成功码统一为 code === 'A00000'。
  *
  * @author ydsz-team
  * @auto-generated 请勿手动修改；后端契约变更后执行 {@code python bash/gen-contract.py system} 重新生成
diff --git a/apps/system-web/src/api/dict.ts b/apps/system-web/src/api/dict.ts
index b7cdac42f..a3f8bdebf 100644
--- a/apps/system-web/src/api/dict.ts
+++ b/apps/system-web/src/api/dict.ts
@@ -4,7 +4,7 @@
  * DictController API 封装（auto-generated by bash/gen-contract.py）
  *
  * <p>对应后端 {@code DictController}，共 6 个端点。
- * <p>路径规范: /api/system/**（kebab-case），成功码统一为 code === 'A00000'。
+ * <p>路径规范: /api/v1/system/**（kebab-case），成功码统一为 code === 'A00000'。
  *
  * @author ydsz-team
  * @auto-generated 请勿手动修改；后端契约变更后执行 {@code python bash/gen-contract.py system} 重新生成
diff --git a/apps/system-web/src/api/dictItem.ts b/apps/system-web/src/api/dictItem.ts
index 51a10c84b..0aef3b115 100644
--- a/apps/system-web/src/api/dictItem.ts
+++ b/apps/system-web/src/api/dictItem.ts
@@ -4,7 +4,7 @@
  * DictItemController API 封装（auto-generated by bash/gen-contract.py）
  *
  * <p>对应后端 {@code DictItemController}，共 10 个端点。
- * <p>路径规范: /api/system/**（kebab-case），成功码统一为 code === 'A00000'。
+ * <p>路径规范: /api/v1/system/**（kebab-case），成功码统一为 code === 'A00000'。
  *
  * @author ydsz-team
  * @auto-generated 请勿手动修改；后端契约变更后执行 {@code python bash/gen-contract.py system} 重新生成
diff --git a/apps/system-web/src/api/dictVersion.ts b/apps/system-web/src/api/dictVersion.ts
index 5306fd452..e77d5f1d0 100644
--- a/apps/system-web/src/api/dictVersion.ts
+++ b/apps/system-web/src/api/dictVersion.ts
@@ -4,7 +4,7 @@
  * DictVersionController API 封装（auto-generated by bash/gen-contract.py）
  *
  * <p>对应后端 {@code DictVersionController}，共 3 个端点。
- * <p>路径规范: /api/system/**（kebab-case），成功码统一为 code === 'A00000'。
+ * <p>路径规范: /api/v1/system/**（kebab-case），成功码统一为 code === 'A00000'。
  *
  * @author ydsz-team
  * @auto-generated 请勿手动修改；后端契约变更后执行 {@code python bash/gen-contract.py system} 重新生成
diff --git a/apps/system-web/src/api/frontendInit.ts b/apps/system-web/src/api/frontendInit.ts
index 49ffe9fc0..e39202ad9 100644
--- a/apps/system-web/src/api/frontendInit.ts
+++ b/apps/system-web/src/api/frontendInit.ts
@@ -4,7 +4,7 @@
  * FrontendInitController API 封装（auto-generated by bash/gen-contract.py）
  *
  * <p>对应后端 {@code FrontendInitController}，共 2 个端点。
- * <p>路径规范: /api/system/**（kebab-case），成功码统一为 code === 'A00000'。
+ * <p>路径规范: /api/v1/system/**（kebab-case），成功码统一为 code === 'A00000'。
  *
  * @author ydsz-team
  * @auto-generated 请勿手动修改；后端契约变更后执行 {@code python bash/gen-contract.py system} 重新生成
diff --git a/apps/system-web/src/api/index.ts b/apps/system-web/src/api/index.ts
index 646c1c28a..59baeb92e 100644
--- a/apps/system-web/src/api/index.ts
+++ b/apps/system-web/src/api/index.ts
@@ -72,3 +72,5 @@ export type {
   ImportResultVO,
   FrontendInitVO,
 } from './models';
+export * from './core';
+export * from './models';
diff --git a/apps/system-web/src/api/internalApi.ts b/apps/system-web/src/api/internalApi.ts
index 06863a992..549b3f684 100644
--- a/apps/system-web/src/api/internalApi.ts
+++ b/apps/system-web/src/api/internalApi.ts
@@ -4,7 +4,7 @@
  * InternalApiController API 封装（auto-generated by bash/gen-contract.py）
  *
  * <p>对应后端 {@code InternalApiController}，共 4 个端点。
- * <p>路径规范: /api/system/**（kebab-case），成功码统一为 code === 'A00000'。
+ * <p>路径规范: /api/v1/system/**（kebab-case），成功码统一为 code === 'A00000'。
  *
  * @author ydsz-team
  * @auto-generated 请勿手动修改；后端契约变更后执行 {@code python bash/gen-contract.py system} 重新生成
diff --git a/apps/system-web/src/api/models.ts b/apps/system-web/src/api/models.ts
index c9a0525bc..be390bb84 100644
--- a/apps/system-web/src/api/models.ts
+++ b/apps/system-web/src/api/models.ts
@@ -38,6 +38,30 @@ export interface PageQuery {
   pageSize?: number;
 }
 
+/**
+ * 接口权限分页查询参数
+ *
+ * 对应 `ydsz_sys_api_permission` 表的分页查询条件，由 Controller 接收并透传给 `ApiPermissionService.page()`。
+ * 继承自 PageQuery，自带 `pageNum` / `pageSize` / `orderBy` / `sort` 等通用分页参数。
+ * 字段语义：
+ * `apiCode` — 权限码模糊匹配（`LIKE %xxx%`）
+ * `apiName` — 接口名称模糊匹配
+ * `controllerClass` — Controller 类名模糊匹配
+ * `status` — 启用状态精确匹配（`=`），可空
+ * 多租户：租户过滤由 MyBatis 拦截器自动注入。
+ */
+export interface ApiPermissionQuery {
+  serialVersionUID?: number;
+  /** 权限码模糊匹配 */
+  apiCode?: string;
+  /** 接口名称模糊匹配 */
+  apiName?: string;
+  /** Controller 类名模糊匹配 */
+  controllerClass?: string;
+  /** 状态精确匹配 */
+  status?: string;
+}
+
 /**
  * 应用信息分页查询参数
  *
@@ -84,6 +108,50 @@ export interface AppInfoDTO {
   status?: string;
 }
 
+/**
+ * 配置变更审批单据查询参数。
+ *
+ * 支持分页、状态筛选和资源类型筛选。
+ */
+export interface ConfigApprovalQuery {
+  /** 默认每页大小 */
+  DEFAULT_PAGE_SIZE?: number;
+  /** 页码（从 1 开始） */
+  pageNum?: number;
+  /** 每页大小 */
+  pageSize?: number;
+  /** 审批状态筛选（PENDING / APPROVED / REJECTED / WITHDRAWN，为空表示全部） */
+  status?: string;
+  /** 资源类型筛选（CONFIG / DICT / VARIABLE，为空表示全部） */
+  resourceType?: string;
+  /** 按发起人 ID 筛选（查询「我已发起的」时使用） */
+  submitterId?: string;
+  /** 按当前审批人 ID 筛选（查询「待我审批的」时使用） */
+  approverId?: string;
+}
+
+/**
+ * 配置变更提交审批请求 DTO。
+ *
+ * 前端在配置管理页面发起审批流时提交，包含资源标识、变更前后值和原因。
+ */
+export interface ConfigApprovalSubmitDTO {
+  /** 资源类型（CONFIG / DICT / VARIABLE） */
+  resourceType?: string;
+  /** 资源唯一标识（如配置键、字典类型编码、变量键） */
+  resourceKey?: string;
+  /** 资源分组（仅 CONFIG 类型有值） */
+  resourceGroup?: string;
+  /** 变更操作类型（CREATE / UPDATE / DELETE） */
+  changeType?: string;
+  /** 变更前的 JSON 值（CREATE 时为空） */
+  beforeJson?: string;
+  /** 变更后的 JSON 值（DELETE 时为空） */
+  afterJson?: string;
+  /** 变更原因 */
+  reason?: string;
+}
+
 /**
  * 系统配置分页查询参数
  *
@@ -118,8 +186,8 @@ export interface ConfigPageQuery {
  * `configValue` — 配置值
  * `valueType` — 值类型: STRING/NUMBER/BOOLEAN/JSON
  * `defaultValue` — 默认值（配置未设置时使用）
- * `isPublic` — 是否对前端公开: 1 公开 / 0 仅后端
- * `sortOrder` — 排序序号
+ * `public` — 是否对前端公开: true 公开 / false 仅后端
+ * `sort` — 排序序号
  * `status` — 启用状态: ENABLED/DISABLED
  */
 export interface ConfigDTO {
@@ -130,8 +198,8 @@ export interface ConfigDTO {
   valueType?: string;
   defaultValue?: string;
   description?: string;
-  isPublic?: number;
-  sortOrder?: number;
+  isPublic?: boolean;
+  sort?: number;
   status?: string;
 }
 
@@ -148,13 +216,13 @@ export interface ConfigBatchDTO {
  * 系统配置 VO（视图对象）
  *
  * 对应 `ydsz_sys_config` 表的展示视图，是「系统配置中心」列表 / 详情接口的响应载体。
- * 由 com.njydsz.system.infra.converter.SystemConverter 从 {@link
- * com.njydsz.system.infra.entity.Config} 实体转换而来。
+ * 由 SystemConverter 从 {@link
+ * Config} 实体转换而来。
  * 字段语义：
  * `configValue` — 配置值，序列化时按 `valueType` 转换： `STRING` 原样输出；{@code NUMBER /
  * BOOLEAN / JSON} 解析为对应类型
  * `defaultValue` — 默认值（`configValue` 为空时回退）
- * `isPublic` — 是否对前端公开：`1` 公开 / `0` 仅后端； 前端「公开配置」接口仅返回 `isPublic=1`
+ * `isPublic` — 是否对前端公开：`true` 公开 / `false` 仅后端； 前端「公开配置」接口仅返回 `isPublic=true`
  * 的项
  * 使用场景：
  * 配置中心列表 / 详情 / 编辑回显
@@ -173,8 +241,8 @@ export interface ConfigVO {
   valueType?: string;
   defaultValue?: string;
   description?: string;
-  isPublic?: number;
-  sortOrder?: number;
+  isPublic?: boolean;
+  sort?: number;
   status?: string;
 }
 
@@ -244,7 +312,7 @@ export interface DictItemPageQuery {
  * `itemCode` — 字典项编码
  * `itemValue` — 字典项展示值
  * `parentId` — 父级 ID
- * `sortOrder` — 排序号
+ * `sort` — 排序号
  * `description` — 字典项业务说明
  * `extJson` — 扩展属性 JSON
  * `status` — 启用状态: ENABLED/DISABLED
@@ -255,7 +323,7 @@ export interface DictItemDTO {
   typeCode?: string;
   itemCode?: string;
   itemValue?: string;
-  sortOrder?: number;
+  sort?: number;
   description?: string;
   extJson?: string;
   status?: string;
@@ -282,7 +350,7 @@ export interface DictItemBatchDTO {
  * `itemCode` — 字典项编码（业务存储值，`<el-option :value="...">`）
  * `itemValue` — 字典项展示值（前端展示文本）
  * `parentId` — 父级 ID（`0` = 根），支持「省 / 市 / 区县」三级级联
- * `sortOrder` — 同类型内排序号（升序），直接作为 TreeBuilder 排序字段
+ * `sort` — 同类型内排序号（升序），直接作为 TreeBuilder 排序字段
  * `extJson` — 扩展属性 JSON（如地区码、颜色值、图标等），前端按需解析
  * 使用场景：
  * 字典中心列表 / 详情
@@ -308,7 +376,7 @@ export interface DictItemVO {
   itemCode?: string;
   itemValue?: string;
   /** 排序号（同类型内升序），直接作为 TreeBuilder 排序字段 */
-  sortOrder?: number;
+  sort?: number;
   description?: string;
   extJson?: string;
   status?: string;
@@ -375,6 +443,35 @@ export interface AppValidateRequest {
   appSecret?: string;
 }
 
+/**
+ * 场景化二级认证请求体（P0-2 标准化）。
+ *
+ * 前端调用 `Post /api/auth/secondary-auth` 接口时传入，包含当前用户密码和目标场景标识。
+ */
+export interface SecondaryAuthRequest {
+  /**
+   * 当前登录用户的明文密码。
+   * 用于验证用户身份，验证通过后写入场景化安全标记。
+   */
+  password?: string;
+  /**
+   * 场景标识（scene）。
+   * 用于区分不同业务场景的二级认证，每个场景独立验证、独立过期。
+   * 常用值：`password_change`、`role_assign`、`data_export`、`tenant_config`
+   */
+  scene?: string;
+  /**
+   * 二级认证有效期（秒）。
+   * 默认 300 秒（5 分钟）。CRITICAL 级别会自动缩短为 40%。
+   */
+  ttlSeconds?: number;
+  /**
+   * 敏感操作等级。
+   * 默认 HIGH。CRITICAL 级别使用更短的验证窗口（TTL 的 40%，最小 60 秒）。
+   */
+  level?: 'MEDIUM' | 'HIGH' | 'CRITICAL';
+}
+
 /**
  * 租户分页查询参数
  *
@@ -436,14 +533,14 @@ export interface TenantPlanPageQuery {
  * 创建时 `id` 为空（由雪花算法自动生成），更新时 `id` 必填。
  * 字段语义：
  * `planCode` — 套餐编码，全局唯一标识
- * `sortOrder` — 排序号（升序，影响前端套餐选择器顺序）
+ * `sort` — 排序号（升序，影响前端套餐选择器顺序）
  */
 export interface TenantPlanDTO {
   id?: string;
   planCode?: string;
   planName?: string;
   description?: string;
-  sortOrder?: number;
+  sort?: number;
   quotaJson?: string;
   featureJson?: string;
   status?: string;
@@ -494,6 +591,34 @@ export interface VariableDTO {
   status?: string;
 }
 
+/**
+ * 接口权限 VO（视图对象）
+ *
+ * 对应 `ydsz_sys_api_permission` 表的展示视图，是「接口权限注册中心」列表 / 详情接口的响应载体。
+ * 字段语义：
+ * `apiCode` — 权限码（如 sys:config:list）
+ * `httpMethod` — HTTP 方法
+ * `urlPattern` — URL 模式
+ * `controllerClass` — Controller 完全限定名
+ * `methodName` — Controller 方法名
+ * `status` — 启用状态: ENABLED/DISABLED
+ */
+export interface ApiPermissionVO {
+  id?: string;
+  apiCode?: string;
+  apiName?: string;
+  httpMethod?: string;
+  urlPattern?: string;
+  controllerClass?: string;
+  methodName?: string;
+  description?: string;
+  status?: string;
+  createdBy?: string;
+  createdAt?: string;
+  updatedBy?: string;
+  updatedAt?: string;
+}
+
 /**
  * 应用注册 VO
  *
@@ -565,6 +690,18 @@ export interface AuditLog {
   requestParams?: string;
   /** 响应结果（已脱敏/截断；默认不记录） */
   responseResult?: string;
+  /**
+   * 变更前快照（JSON）。
+   * 仅当 com.njydsz.common.audit.annotation.Audit#recordDiff() = true 时
+   * 在方法执行前由切面查询记录。用于 diff 追溯（如：更新前的用户信息）。
+   */
+  diffBeforeSnapshot?: string;
+  /**
+   * 变更后快照（JSON）。
+   * 方法执行后由切面捕获（含方法返回值）。与 #diffBeforeSnapshot 配合
+   * 记录完整的变更 diff。
+   */
+  diffAfterSnapshot?: string;
   /** 错误信息（业务方法抛异常时记录） */
   errorMessage?: string;
   /** 执行耗时（毫秒） */
@@ -580,21 +717,52 @@ export interface AuditLog {
 }
 
 /**
- * 导入结果 VO
+ * 配置变更审批单视图对象（返回前端）。
+ *
+ * 封装审批单的完整展示信息，包括审计日志时间线（JSON 解析后填充）。
+ */
+export interface ConfigApprovalVO {
+  serialVersionUID?: number;
+  /** 审批单唯一 ID */
+  id?: string;
+  /** 审批标题 */
+  title?: string;
+  /** 资源类型（CONFIG / DICT / VARIABLE） */
+  resourceType?: string;
+  /** 资源唯一标识 */
+  resourceKey?: string;
+  /** 资源分组 */
+  resourceGroup?: string;
+  /** 变更操作类型（CREATE / UPDATE / DELETE） */
+  changeType?: string;
+  /** 变更前的 JSON 值 */
+  beforeJson?: string;
+  /** 变更后的 JSON 值 */
+  afterJson?: string;
+  /** 审批状态（PENDING / APPROVED / REJECTED / WITHDRAWN） */
+  status?: string;
+  /** 发起人 ID */
+  submitterId?: string;
+  /** 发起人姓名 */
+  submitterName?: string;
+  /** 发起时间 */
+  submittedAt?: string;
+  /** 当前审批人姓名 */
+  currentApproverName?: string;
+  /** 变更原因 */
+  reason?: string;
+  /** 拒绝原因 */
+  rejectionReason?: string;
+  /** 审批单关闭时间 */
+  closedAt?: string;
+}
+
+/**
+ * 导入结果 VO（系统模块）。
  *
- * 封装 Excel 导入的结果统计信息。
+ * 封装 Excel 导入的结果统计信息，继承通用 ExcelImportResult 基类。
  */
 export interface ImportResultVO {
-  /** 总行数 */
-  totalCount?: number;
-  /** 成功数 */
-  successCount?: number;
-  /** 失败数 */
-  failCount?: number;
-  /** 跳过的行数（重复或无效数据） */
-  skipCount?: number;
-  /** 错误信息列表 */
-  errors?: string[];
   /** 导入结果消息 */
   message?: string;
 }
@@ -615,6 +783,36 @@ export interface EntityVersionVO {
   snapshotJson?: string;
 }
 
+/**
+ * 工作台概览统计项 VO
+ *
+ * 供 `GET /api/dashboard/overview` 返回，对齐前端
+ * `OverviewItem` 契约（analytics 页顶部统计卡片）。
+ */
+export interface DashboardOverviewItemVO {
+  /** 统计项标题（如「租户总数」） */
+  title?: string;
+  /** 累计总值标题（如「累计租户」） */
+  totalTitle?: string;
+  /** 累计总值 */
+  totalValue?: number;
+  /** 今日/增量值 */
+  value?: number;
+}
+
+/**
+ * 工作台聚合数据 VO
+ *
+ * 供 `GET /api/dashboard/workspace` 返回，对齐前端 `WorkspaceData` 契约。
+ * 当前版本提供后端可真实计算的「时段化问候语」；项目/待办/动态等列表字段由前端
+ * 按字段粒度回退本地默认值（前端 useWorkspaceData 已内置该合并逻辑），
+ * 待各业务模块沉淀出真实的待办/动态聚合能力后再逐字段接入。
+ */
+export interface DashboardWorkspaceVO {
+  /** 问候文案（按时段 + 当前用户名生成，如「下午好，admin」） */
+  greeting?: string;
+}
+
 /**
  * 字典类型 VO（视图对象）
  *
@@ -692,7 +890,7 @@ export interface TenantVO {
  * 对应 `ydsz_sys_tenant_plan` 表的展示视图，是「套餐管理」列表 / 详情接口的响应载体。
  * 字段语义：
  * `planCode` — 套餐编码，全局唯一标识
- * `sortOrder` — 排序号（升序，影响前端套餐选择器顺序）
+ * `sort` — 排序号（升序，影响前端套餐选择器顺序）
  * 注意：本类为视图对象，不包含输入校验逻辑。输入校验由 {@link
  * com.njydsz.system.domain.dto.TenantPlanDTO} 负责。
  */
@@ -701,7 +899,7 @@ export interface TenantPlanVO {
   planCode?: string;
   planName?: string;
   description?: string;
-  sortOrder?: number;
+  sort?: number;
   quotaJson?: string;
   featureJson?: string;
 }
diff --git a/apps/system-web/src/api/sdk/openapi.json b/apps/system-web/src/api/sdk/openapi.json
index 9ab1b4727..907d51b54 100644
--- a/apps/system-web/src/api/sdk/openapi.json
+++ b/apps/system-web/src/api/sdk/openapi.json
@@ -6,7 +6,175 @@
   "description": "static contract extracted from ydsz-cloud system"
  },
  "paths": {
-  "/api/v1/app/page": {
+  "/api/permission/api/page": {
+   "get": {
+    "operationId": "page",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    },
+    "parameters": [
+     {
+      "name": "query",
+      "in": "query",
+      "required": false,
+      "schema": {
+       "$ref": "#/components/schemas/ApiPermissionQuery"
+      }
+     }
+    ]
+   }
+  },
+  "/api/permission/api/{id}": {
+   "get": {
+    "operationId": "getById",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    },
+    "parameters": [
+     {
+      "name": "id",
+      "in": "path",
+      "required": true,
+      "schema": {
+       "type": "string"
+      }
+     }
+    ]
+   },
+   "delete": {
+    "operationId": "remove",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    },
+    "parameters": [
+     {
+      "name": "id",
+      "in": "path",
+      "required": true,
+      "schema": {
+       "type": "string"
+      }
+     }
+    ]
+   }
+  },
+  "/api/permission/api/scan": {
+   "post": {
+    "operationId": "triggerScan",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    }
+   }
+  },
+  "/api/permission/api/{id}/enable": {
+   "post": {
+    "operationId": "enable",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    },
+    "parameters": [
+     {
+      "name": "id",
+      "in": "path",
+      "required": true,
+      "schema": {
+       "type": "string"
+      }
+     }
+    ]
+   }
+  },
+  "/api/permission/api/{id}/disable": {
+   "post": {
+    "operationId": "disable",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    },
+    "parameters": [
+     {
+      "name": "id",
+      "in": "path",
+      "required": true,
+      "schema": {
+       "type": "string"
+      }
+     }
+    ]
+   }
+  },
+  "/api/app/page": {
    "get": {
     "operationId": "page",
     "tags": [
@@ -36,7 +204,7 @@
     ]
    }
   },
-  "/api/v1/app/{id}": {
+  "/api/app/{id}": {
    "get": {
     "operationId": "getById",
     "tags": [
@@ -94,7 +262,7 @@
     ]
    }
   },
-  "/api/v1/app": {
+  "/api/app": {
    "post": {
     "operationId": "save",
     "tags": [
@@ -152,7 +320,7 @@
     }
    }
   },
-  "/api/v1/admin/audit/logs": {
+  "/api/admin/audit/logs": {
    "get": {
     "operationId": "queryByTimeRange",
     "tags": [
@@ -207,12 +375,289 @@
        "format": "int32"
       }
      }
-    ]
+    ]
+   }
+  },
+  "/api/admin/audit/operator/{operatorId}": {
+   "get": {
+    "operationId": "queryByOperator",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    },
+    "parameters": [
+     {
+      "name": "operatorId",
+      "in": "path",
+      "required": true,
+      "schema": {
+       "type": "string"
+      }
+     },
+     {
+      "name": "page",
+      "in": "query",
+      "required": false,
+      "schema": {
+       "type": "integer",
+       "format": "int32"
+      }
+     },
+     {
+      "name": "size",
+      "in": "query",
+      "required": false,
+      "schema": {
+       "type": "integer",
+       "format": "int32"
+      }
+     }
+    ]
+   }
+  },
+  "/api/admin/audit/action/{action}": {
+   "get": {
+    "operationId": "queryByAction",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    },
+    "parameters": [
+     {
+      "name": "action",
+      "in": "path",
+      "required": true,
+      "schema": {
+       "type": "integer",
+       "format": "int32"
+      }
+     },
+     {
+      "name": "page",
+      "in": "query",
+      "required": false,
+      "schema": {
+       "type": "integer",
+       "format": "int32"
+      }
+     },
+     {
+      "name": "size",
+      "in": "query",
+      "required": false,
+      "schema": {
+       "type": "integer",
+       "format": "int32"
+      }
+     }
+    ]
+   }
+  },
+  "/api/admin/audit/trace/{traceId}": {
+   "get": {
+    "operationId": "queryByTraceId",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    },
+    "parameters": [
+     {
+      "name": "traceId",
+      "in": "path",
+      "required": true,
+      "schema": {
+       "type": "string"
+      }
+     }
+    ]
+   }
+  },
+  "/api/admin/audit/{id}": {
+   "get": {
+    "operationId": "getById",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    },
+    "parameters": [
+     {
+      "name": "id",
+      "in": "path",
+      "required": true,
+      "schema": {
+       "type": "string"
+      }
+     }
+    ]
+   }
+  },
+  "/api/config/approval/pending": {
+   "get": {
+    "operationId": "listPending",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    },
+    "parameters": [
+     {
+      "name": "query",
+      "in": "query",
+      "required": false,
+      "schema": {
+       "$ref": "#/components/schemas/ConfigApprovalQuery"
+      }
+     }
+    ]
+   }
+  },
+  "/api/config/approval/submitted": {
+   "get": {
+    "operationId": "listSubmitted",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    },
+    "parameters": [
+     {
+      "name": "query",
+      "in": "query",
+      "required": false,
+      "schema": {
+       "$ref": "#/components/schemas/ConfigApprovalQuery"
+      }
+     }
+    ]
+   }
+  },
+  "/api/config/approval/list": {
+   "get": {
+    "operationId": "listAll",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    },
+    "parameters": [
+     {
+      "name": "query",
+      "in": "query",
+      "required": false,
+      "schema": {
+       "$ref": "#/components/schemas/ConfigApprovalQuery"
+      }
+     }
+    ]
+   }
+  },
+  "/api/config/approval/submit": {
+   "post": {
+    "operationId": "submit",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    },
+    "requestBody": {
+     "required": true,
+     "content": {
+      "application/json": {
+       "schema": {
+        "$ref": "#/components/schemas/ConfigApprovalSubmitDTO"
+       }
+      }
+     }
+    }
    }
   },
-  "/api/v1/admin/audit/operator/{operatorId}": {
-   "get": {
-    "operationId": "queryByOperator",
+  "/api/config/approval/{id}/approve": {
+   "post": {
+    "operationId": "approve",
     "tags": [
      "system"
     ],
@@ -230,37 +675,29 @@
     },
     "parameters": [
      {
-      "name": "operatorId",
+      "name": "id",
       "in": "path",
       "required": true,
       "schema": {
        "type": "string"
       }
-     },
-     {
-      "name": "page",
-      "in": "query",
-      "required": false,
-      "schema": {
-       "type": "integer",
-       "format": "int32"
-      }
-     },
-     {
-      "name": "size",
-      "in": "query",
-      "required": false,
-      "schema": {
-       "type": "integer",
-       "format": "int32"
+     }
+    ],
+    "requestBody": {
+     "required": true,
+     "content": {
+      "application/json": {
+       "schema": {
+        "type": "object"
+       }
       }
      }
-    ]
+    }
    }
   },
-  "/api/v1/admin/audit/action/{action}": {
-   "get": {
-    "operationId": "queryByAction",
+  "/api/config/approval/{id}/reject": {
+   "post": {
+    "operationId": "reject",
     "tags": [
      "system"
     ],
@@ -278,38 +715,29 @@
     },
     "parameters": [
      {
-      "name": "action",
+      "name": "id",
       "in": "path",
       "required": true,
       "schema": {
-       "type": "integer",
-       "format": "int32"
-      }
-     },
-     {
-      "name": "page",
-      "in": "query",
-      "required": false,
-      "schema": {
-       "type": "integer",
-       "format": "int32"
+       "type": "string"
       }
-     },
-     {
-      "name": "size",
-      "in": "query",
-      "required": false,
-      "schema": {
-       "type": "integer",
-       "format": "int32"
+     }
+    ],
+    "requestBody": {
+     "required": true,
+     "content": {
+      "application/json": {
+       "schema": {
+        "type": "object"
+       }
       }
      }
-    ]
+    }
    }
   },
-  "/api/v1/admin/audit/trace/{traceId}": {
-   "get": {
-    "operationId": "queryByTraceId",
+  "/api/config/approval/{id}/withdraw": {
+   "post": {
+    "operationId": "withdraw",
     "tags": [
      "system"
     ],
@@ -327,7 +755,7 @@
     },
     "parameters": [
      {
-      "name": "traceId",
+      "name": "id",
       "in": "path",
       "required": true,
       "schema": {
@@ -337,9 +765,9 @@
     ]
    }
   },
-  "/api/v1/admin/audit/{id}": {
+  "/api/config/approval/{id}": {
    "get": {
-    "operationId": "getById",
+    "operationId": "detail",
     "tags": [
      "system"
     ],
@@ -367,7 +795,7 @@
     ]
    }
   },
-  "/api/v1/config/page": {
+  "/api/config/page": {
    "get": {
     "operationId": "page",
     "tags": [
@@ -397,7 +825,7 @@
     ]
    }
   },
-  "/api/v1/config/cursor": {
+  "/api/config/cursor": {
    "get": {
     "operationId": "pageByCursor",
     "tags": [
@@ -452,7 +880,7 @@
     ]
    }
   },
-  "/api/v1/config/{id}": {
+  "/api/config/{id}": {
    "get": {
     "operationId": "getById",
     "tags": [
@@ -510,7 +938,7 @@
     ]
    }
   },
-  "/api/v1/config": {
+  "/api/config": {
    "post": {
     "operationId": "save",
     "tags": [
@@ -568,7 +996,7 @@
     }
    }
   },
-  "/api/v1/config/batch": {
+  "/api/config/batch": {
    "post": {
     "operationId": "batchSave",
     "tags": [
@@ -598,7 +1026,7 @@
     }
    }
   },
-  "/api/v1/config/key/{configKey}": {
+  "/api/config/key/{configKey}": {
    "get": {
     "operationId": "getByKey",
     "tags": [
@@ -628,7 +1056,7 @@
     ]
    }
   },
-  "/api/v1/config/group/{configGroup}": {
+  "/api/config/group/{configGroup}": {
    "get": {
     "operationId": "getConfigsByGroup",
     "tags": [
@@ -658,7 +1086,7 @@
     ]
    }
   },
-  "/api/v1/config/public": {
+  "/api/config/public": {
    "get": {
     "operationId": "listPublicConfigs",
     "tags": [
@@ -678,7 +1106,7 @@
     }
    }
   },
-  "/api/v1/config/export": {
+  "/api/config/export": {
    "get": {
     "operationId": "exportConfigs",
     "tags": [
@@ -717,7 +1145,7 @@
     ]
    }
   },
-  "/api/v1/config/import": {
+  "/api/config/import": {
    "post": {
     "operationId": "importConfigs",
     "tags": [
@@ -747,7 +1175,7 @@
     ]
    }
   },
-  "/api/v1/config/version/{resourceKey}": {
+  "/api/config/version/{resourceKey}": {
    "get": {
     "operationId": "listByResourceKey",
     "tags": [
@@ -777,7 +1205,7 @@
     ]
    }
   },
-  "/api/v1/config/version/{resourceKey}/rollback": {
+  "/api/config/version/{resourceKey}/rollback": {
    "post": {
     "operationId": "rollback",
     "tags": [
@@ -823,7 +1251,47 @@
     ]
    }
   },
-  "/api/v1/dict/type/page": {
+  "/api/dashboard/overview": {
+   "get": {
+    "operationId": "overview",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    }
+   }
+  },
+  "/api/dashboard/workspace": {
+   "get": {
+    "operationId": "workspace",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    }
+   }
+  },
+  "/api/dict/type/page": {
    "get": {
     "operationId": "page",
     "tags": [
@@ -853,7 +1321,7 @@
     ]
    }
   },
-  "/api/v1/dict/type/{id}": {
+  "/api/dict/type/{id}": {
    "get": {
     "operationId": "getById",
     "tags": [
@@ -911,7 +1379,7 @@
     ]
    }
   },
-  "/api/v1/dict/type": {
+  "/api/dict/type": {
    "post": {
     "operationId": "save",
     "tags": [
@@ -969,7 +1437,7 @@
     }
    }
   },
-  "/api/v1/dict/type/all": {
+  "/api/dict/type/all": {
    "get": {
     "operationId": "listAll",
     "tags": [
@@ -989,7 +1457,7 @@
     }
    }
   },
-  "/api/v1/dict/item/page": {
+  "/api/dict/item/page": {
    "get": {
     "operationId": "page",
     "tags": [
@@ -1019,7 +1487,7 @@
     ]
    }
   },
-  "/api/v1/dict/item/{id}": {
+  "/api/dict/item/{id}": {
    "get": {
     "operationId": "getById",
     "tags": [
@@ -1077,7 +1545,7 @@
     ]
    }
   },
-  "/api/v1/dict/item/lookup": {
+  "/api/dict/item/lookup": {
    "get": {
     "operationId": "lookup",
     "tags": [
@@ -1115,7 +1583,7 @@
     ]
    }
   },
-  "/api/v1/dict/item/type/{typeCode}": {
+  "/api/dict/item/type/{typeCode}": {
    "get": {
     "operationId": "listByType",
     "tags": [
@@ -1145,7 +1613,7 @@
     ]
    }
   },
-  "/api/v1/dict/item/children/{parentId}": {
+  "/api/dict/item/children/{parentId}": {
    "get": {
     "operationId": "listChildren",
     "tags": [
@@ -1175,7 +1643,7 @@
     ]
    }
   },
-  "/api/v1/dict/item/tree/{typeCode}": {
+  "/api/dict/item/tree/{typeCode}": {
    "get": {
     "operationId": "buildTree",
     "tags": [
@@ -1205,7 +1673,7 @@
     ]
    }
   },
-  "/api/v1/dict/item": {
+  "/api/dict/item": {
    "post": {
     "operationId": "save",
     "tags": [
@@ -1263,7 +1731,7 @@
     }
    }
   },
-  "/api/v1/dict/item/batch": {
+  "/api/dict/item/batch": {
    "post": {
     "operationId": "batchSave",
     "tags": [
@@ -1293,7 +1761,7 @@
     }
    }
   },
-  "/api/v1/dict/version/{typeCode}": {
+  "/api/dict/version/{typeCode}": {
    "get": {
     "operationId": "listByTypeCode",
     "tags": [
@@ -1323,7 +1791,7 @@
     ]
    }
   },
-  "/api/v1/dict/version/page": {
+  "/api/dict/version/page": {
    "get": {
     "operationId": "pageByTypeCode",
     "tags": [
@@ -1353,7 +1821,7 @@
     ]
    }
   },
-  "/api/v1/dict/version/{typeCode}/rollback": {
+  "/api/dict/version/{typeCode}/rollback": {
    "post": {
     "operationId": "rollback",
     "tags": [
@@ -1399,7 +1867,27 @@
     ]
    }
   },
-  "/api/v1/system/init": {
+  "/api/feature-flags/me": {
+   "get": {
+    "operationId": "me",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    }
+   }
+  },
+  "/api/system/init": {
    "get": {
     "operationId": "init",
     "tags": [
@@ -1419,9 +1907,132 @@
     }
    }
   },
-  "/api/v1/system/init/dicts": {
-   "get": {
-    "operationId": "initWithDicts",
+  "/api/system/init/dicts": {
+   "get": {
+    "operationId": "initWithDicts",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    },
+    "parameters": [
+     {
+      "name": "dictTypes",
+      "in": "query",
+      "required": false,
+      "schema": {
+       "type": "array",
+       "items": {
+        "type": "string"
+       }
+      }
+     }
+    ]
+   }
+  },
+  "/api/internal/config/get": {
+   "post": {
+    "operationId": "getConfig",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    },
+    "requestBody": {
+     "required": true,
+     "content": {
+      "application/json": {
+       "schema": {
+        "$ref": "#/components/schemas/ConfigGetRequest"
+       }
+      }
+     }
+    }
+   }
+  },
+  "/api/internal/dict/item": {
+   "post": {
+    "operationId": "getDictItem",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    },
+    "requestBody": {
+     "required": true,
+     "content": {
+      "application/json": {
+       "schema": {
+        "$ref": "#/components/schemas/DictItemGetRequest"
+       }
+      }
+     }
+    }
+   }
+  },
+  "/api/internal/dict/list": {
+   "post": {
+    "operationId": "listDictItems",
+    "tags": [
+     "system"
+    ],
+    "responses": {
+     "200": {
+      "description": "OK",
+      "content": {
+       "application/json": {
+        "schema": {
+         "$ref": "#/components/schemas/YdszResponse"
+        }
+       }
+      }
+     }
+    },
+    "requestBody": {
+     "required": true,
+     "content": {
+      "application/json": {
+       "schema": {
+        "$ref": "#/components/schemas/DictListRequest"
+       }
+      }
+     }
+    }
+   }
+  },
+  "/api/internal/app/validate": {
+   "post": {
+    "operationId": "validateClient",
     "tags": [
      "system"
     ],
@@ -1437,24 +2048,21 @@
       }
      }
     },
-    "parameters": [
-     {
-      "name": "dictTypes",
-      "in": "query",
-      "required": false,
-      "schema": {
-       "type": "array",
-       "items": {
-        "type": "string"
+    "requestBody": {
+     "required": true,
+     "content": {
+      "application/json": {
+       "schema": {
+        "$ref": "#/components/schemas/AppValidateRequest"
        }
       }
      }
-    ]
+    }
    }
   },
-  "/api/internal/config/get": {
-   "post": {
-    "operationId": "getConfig",
+  "/api/system/metrics/dashboard": {
+   "get": {
+    "operationId": "dashboard",
     "tags": [
      "system"
     ],
@@ -1469,22 +2077,12 @@
        }
       }
      }
-    },
-    "requestBody": {
-     "required": true,
-     "content": {
-      "application/json": {
-       "schema": {
-        "$ref": "#/components/schemas/ConfigGetRequest"
-       }
-      }
-     }
     }
    }
   },
-  "/api/internal/dict/item": {
-   "post": {
-    "operationId": "getDictItem",
+  "/repeat-submit/token": {
+   "get": {
+    "operationId": "getToken",
     "tags": [
      "system"
     ],
@@ -1500,21 +2098,22 @@
       }
      }
     },
-    "requestBody": {
-     "required": true,
-     "content": {
-      "application/json": {
-       "schema": {
-        "$ref": "#/components/schemas/DictItemGetRequest"
-       }
+    "parameters": [
+     {
+      "name": "ttlMillis",
+      "in": "query",
+      "required": false,
+      "schema": {
+       "type": "integer",
+       "format": "int64"
       }
      }
-    }
+    ]
    }
   },
-  "/api/internal/dict/list": {
+  "/api/auth/secondary-auth": {
    "post": {
-    "operationId": "listDictItems",
+    "operationId": "verify",
     "tags": [
      "system"
     ],
@@ -1535,16 +2134,16 @@
      "content": {
       "application/json": {
        "schema": {
-        "$ref": "#/components/schemas/DictListRequest"
+        "$ref": "#/components/schemas/SecondaryAuthRequest"
        }
       }
      }
     }
    }
   },
-  "/api/internal/app/validate": {
-   "post": {
-    "operationId": "validateClient",
+  "/api/tenant/accessible": {
+   "get": {
+    "operationId": "listAccessible",
     "tags": [
      "system"
     ],
@@ -1559,20 +2158,10 @@
        }
       }
      }
-    },
-    "requestBody": {
-     "required": true,
-     "content": {
-      "application/json": {
-       "schema": {
-        "$ref": "#/components/schemas/AppValidateRequest"
-       }
-      }
-     }
     }
    }
   },
-  "/api/v1/tenant/page": {
+  "/api/tenant/page": {
    "get": {
     "operationId": "page",
     "tags": [
@@ -1602,7 +2191,7 @@
     ]
    }
   },
-  "/api/v1/tenant/{id}": {
+  "/api/tenant/{id}": {
    "get": {
     "operationId": "getById",
     "tags": [
@@ -1660,7 +2249,7 @@
     ]
    }
   },
-  "/api/v1/tenant": {
+  "/api/tenant": {
    "post": {
     "operationId": "save",
     "tags": [
@@ -1738,7 +2327,7 @@
     }
    }
   },
-  "/api/v1/tenant-plan/page": {
+  "/api/tenant-plan/page": {
    "get": {
     "operationId": "page",
     "tags": [
@@ -1768,7 +2357,7 @@
     ]
    }
   },
-  "/api/v1/tenant-plan/list": {
+  "/api/tenant-plan/list": {
    "get": {
     "operationId": "listAll",
     "tags": [
@@ -1788,7 +2377,7 @@
     }
    }
   },
-  "/api/v1/tenant-plan/{id}": {
+  "/api/tenant-plan/{id}": {
    "get": {
     "operationId": "getById",
     "tags": [
@@ -1846,7 +2435,7 @@
     ]
    }
   },
-  "/api/v1/tenant-plan": {
+  "/api/tenant-plan": {
    "post": {
     "operationId": "save",
     "tags": [
@@ -1924,7 +2513,7 @@
     }
    }
   },
-  "/api/v1/tenant-plan/{planId}/menus": {
+  "/api/tenant-plan/{planId}/menus": {
    "get": {
     "operationId": "listMenus",
     "tags": [
@@ -1954,7 +2543,7 @@
     ]
    }
   },
-  "/api/v1/tenant-plan/menus": {
+  "/api/tenant-plan/menus": {
    "post": {
     "operationId": "updateMenus",
     "tags": [
@@ -1984,7 +2573,7 @@
     }
    }
   },
-  "/api/v1/variable/page": {
+  "/api/variable/page": {
    "get": {
     "operationId": "page",
     "tags": [
@@ -2014,7 +2603,7 @@
     ]
    }
   },
-  "/api/v1/variable/{id}": {
+  "/api/variable/{id}": {
    "get": {
     "operationId": "getById",
     "tags": [
@@ -2072,7 +2661,7 @@
     ]
    }
   },
-  "/api/v1/variable/key/{variableKey}": {
+  "/api/variable/key/{variableKey}": {
    "get": {
     "operationId": "getByKey",
     "tags": [
@@ -2102,7 +2691,7 @@
     ]
    }
   },
-  "/api/v1/variable": {
+  "/api/variable": {
    "post": {
     "operationId": "save",
     "tags": [
@@ -2160,7 +2749,7 @@
     }
    }
   },
-  "/api/v1/variable/version/{resourceKey}": {
+  "/api/variable/version/{resourceKey}": {
    "get": {
     "operationId": "listByResourceKey",
     "tags": [
@@ -2190,7 +2779,7 @@
     ]
    }
   },
-  "/api/v1/variable/version/{resourceKey}/rollback": {
+  "/api/variable/version/{resourceKey}/rollback": {
    "post": {
     "operationId": "rollback",
     "tags": [
@@ -2244,7 +2833,8 @@
     "properties": {
      "serialVersionUID": {
       "type": "integer",
-      "format": "int64"
+      "format": "int64",
+      "description": "序列化版本 UID，兼容 Lombok @SuperBuilder 生成的子类序列化。"
      },
      "code": {
       "type": "string",
@@ -2254,6 +2844,10 @@
       "type": "string",
       "description": "返回信息"
      },
+     "level": {
+      "type": "string",
+      "description": "异常级别。\n\n取值来自 com.njydsz.common.exception.enums.ExceptionLevel 枚举的 name：\n\n\nINFO — 静默处理，不展示 toast\nWARN — 轻量 toast 提示（auto-close 3s）\nERROR — toast 提示，需用户点击关闭\nFATAL — 弹窗提示，阻断用户当前操作\n\n\n成功响应不需要级别，为 `null` 时不序列化（通过 `@JsonInclude(NON_NULL)` 控制）。"
+     },
      "data": {
       "type": "object",
       "description": "返回数据\n\n泛型类型 T 无法限定为 Serializable（API 响应可携带任意类型数据）， Java 序列化非主要序列化方式（项目使用 Jackson JSON），此处抑制编译器警告。"
@@ -2289,12 +2883,14 @@
     },
     "description": "统一API返回结果封装类\n\n用于前后端交互的标准返回格式，封装了响应码、消息、数据和时间戳。\n\n响应结构：\n\n\ncode: 响应码，A00000表示成功，其他表示失败\nmsg: 响应消息\ndata: 响应数据\ntimestamp: 响应时间戳\n\n\n使用示例：\n\n<pre>{@code\n// 返回成功\nreturn YdszResponse.success(user);\n\n// 返回失败（走 i18n）\nreturn YdszResponse.error(YdszResultCode.NOT_FOUND);\n}</pre>\n\n@param <T> 数据泛型",
     "x-field-docs": {
+     "serialVersionUID": "序列化版本 UID，兼容 Lombok @SuperBuilder 生成的子类序列化。",
      "SUCCESS": "成功状态码（复用 YdszResultCode#SUCCESS，与错误码体系保持一致）。\n\n该常量表示操作成功（0），用于构造成功响应或判断响应是否成功。",
      "UNKNOWN_CODE": "未知错误状态码（复用 YdszResultCode#UNKNOWN，与错误码体系保持一致）。\n\n该常量表示系统未知错误（C99999），用于与 #SUCCESS 进行反向校验场景。 命名明确区分\"通用错误\"与\"未知错误\"语义，避免与 `error()`\n方法名混淆。",
      "MSG_OPERATION_SUCCESS": "操作成功国际化消息 key（core 模块）。",
      "MSG_OPERATION_FAIL": "操作失败国际化消息 key（core 模块）。",
      "code": "返回编码",
      "msg": "返回信息",
+     "level": "异常级别。\n\n取值来自 com.njydsz.common.exception.enums.ExceptionLevel 枚举的 name：\n\n\nINFO — 静默处理，不展示 toast\nWARN — 轻量 toast 提示（auto-close 3s）\nERROR — toast 提示，需用户点击关闭\nFATAL — 弹窗提示，阻断用户当前操作\n\n\n成功响应不需要级别，为 `null` 时不序列化（通过 `@JsonInclude(NON_NULL)` 控制）。",
      "data": "返回数据\n\n泛型类型 T 无法限定为 Serializable（API 响应可携带任意类型数据）， Java 序列化非主要序列化方式（项目使用 Jackson JSON），此处抑制编译器警告。",
      "traceId": "链路追踪 ID。\n\n首次调用 #getTraceId() 时从 RequestContext/MDC 懒解析， 避免批量/流式场景下每次构造响应对象都产生 MDC 读取开销。",
      "requestId": "请求 ID（用于客户端/前端精准排障，单个请求唯一）。\n\nvolatile + 懒加载：为 `null` 时首次访问从 RequestContext#getRequestId() 解析并缓存。",
@@ -2305,6 +2901,38 @@
      "tid": "ensure traceId resolved first so we know if it's present"
     }
    },
+   "ApiPermissionQuery": {
+    "type": "object",
+    "properties": {
+     "serialVersionUID": {
+      "type": "integer",
+      "format": "int64"
+     },
+     "apiCode": {
+      "type": "string",
+      "description": "权限码模糊匹配"
+     },
+     "apiName": {
+      "type": "string",
+      "description": "接口名称模糊匹配"
+     },
+     "controllerClass": {
+      "type": "string",
+      "description": "Controller 类名模糊匹配"
+     },
+     "status": {
+      "type": "string",
+      "description": "状态精确匹配"
+     }
+    },
+    "description": "接口权限分页查询参数\n\n对应 `ydsz_sys_api_permission` 表的分页查询条件，由 Controller 接收并透传给 `ApiPermissionService.page()`。\n继承自 PageQuery，自带 `pageNum` / `pageSize` / `orderBy` / `sort` 等通用分页参数。\n\n字段语义：\n\n\n`apiCode` — 权限码模糊匹配（`LIKE %xxx%`）\n`apiName` — 接口名称模糊匹配\n`controllerClass` — Controller 类名模糊匹配\n`status` — 启用状态精确匹配（`=`），可空\n\n\n多租户：租户过滤由 MyBatis 拦截器自动注入。",
+    "x-field-docs": {
+     "apiCode": "权限码模糊匹配",
+     "apiName": "接口名称模糊匹配",
+     "controllerClass": "Controller 类名模糊匹配",
+     "status": "状态精确匹配"
+    }
+   },
    "AppInfoPageQuery": {
     "type": "object",
     "properties": {
@@ -2329,25 +2957,32 @@
       "type": "string"
      },
      "appCode": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 64
      },
      "appName": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 128
      },
      "appKey": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 128
      },
      "appSecret": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 256
      },
      "redirectUrl": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 512
      },
      "scopes": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 512
      },
      "boundIps": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 512
      },
      "description": {
       "type": "string"
@@ -2357,7 +2992,109 @@
      }
     },
     "description": "应用注册创建/更新 DTO\n\n对应 `ydsz_sys_app_info` 表的写入参数，对标 OAuth 2.0 客户端注册模型。 创建时 `id` 为空（由雪花算法自动生成），更新时 {@code\nid} 必填。\n\n字段约束：\n\n\n`appCode` — 应用编码，租户内唯一，最长 64 字符\n`appName` — 应用名称，最长 128 字符\n`appKey` — 客户端 ID（`client_id`），最长 128 字符\n`appSecret` — 客户端密钥（`client_secret`），BCrypt 加密存储，最长 256 字符\n`redirectUrl` — 授权回调地址，最长 512 字符，需 URL 合法\n`scopes` — OAuth2 授权范围（CSV），如 `\"user.read,order.write\"`\n`boundIps` — IP 绑定白名单（CSV），如 `\"192.168.1.0/24,10.0.0.1\"`，为空表示不限制\n`status` — 启用状态：`ENABLED / DISABLED`\n\n\n安全约束：\n\n\n`appSecret` 在 Service 层 BCrypt 加密后存储，明文不落库\n接口权限校验：`ydsz:app:create / ydsz:app:update`",
-    "x-field-docs": {}
+    "x-field-docs": {},
+    "required": [
+     "appCode",
+     "appName",
+     "appKey"
+    ]
+   },
+   "ConfigApprovalQuery": {
+    "type": "object",
+    "properties": {
+     "DEFAULT_PAGE_SIZE": {
+      "type": "integer",
+      "format": "int32",
+      "description": "默认每页大小"
+     },
+     "pageNum": {
+      "type": "integer",
+      "format": "int32",
+      "description": "页码（从 1 开始）"
+     },
+     "pageSize": {
+      "type": "integer",
+      "format": "int32",
+      "description": "每页大小"
+     },
+     "status": {
+      "type": "string",
+      "description": "审批状态筛选（PENDING / APPROVED / REJECTED / WITHDRAWN，为空表示全部）"
+     },
+     "resourceType": {
+      "type": "string",
+      "description": "资源类型筛选（CONFIG / DICT / VARIABLE，为空表示全部）"
+     },
+     "submitterId": {
+      "type": "string",
+      "description": "按发起人 ID 筛选（查询「我已发起的」时使用）"
+     },
+     "approverId": {
+      "type": "string",
+      "description": "按当前审批人 ID 筛选（查询「待我审批的」时使用）"
+     }
+    },
+    "description": "配置变更审批单据查询参数。\n\n支持分页、状态筛选和资源类型筛选。",
+    "x-field-docs": {
+     "DEFAULT_PAGE_SIZE": "默认每页大小",
+     "pageNum": "页码（从 1 开始）",
+     "pageSize": "每页大小",
+     "status": "审批状态筛选（PENDING / APPROVED / REJECTED / WITHDRAWN，为空表示全部）",
+     "resourceType": "资源类型筛选（CONFIG / DICT / VARIABLE，为空表示全部）",
+     "submitterId": "按发起人 ID 筛选（查询「我已发起的」时使用）",
+     "approverId": "按当前审批人 ID 筛选（查询「待我审批的」时使用）"
+    }
+   },
+   "ConfigApprovalSubmitDTO": {
+    "type": "object",
+    "properties": {
+     "resourceType": {
+      "type": "string",
+      "description": "资源类型（CONFIG / DICT / VARIABLE）"
+     },
+     "resourceKey": {
+      "type": "string",
+      "description": "资源唯一标识（如配置键、字典类型编码、变量键）",
+      "maxLength": 128
+     },
+     "resourceGroup": {
+      "type": "string",
+      "description": "资源分组（仅 CONFIG 类型有值）",
+      "maxLength": 64
+     },
+     "changeType": {
+      "type": "string",
+      "description": "变更操作类型（CREATE / UPDATE / DELETE）"
+     },
+     "beforeJson": {
+      "type": "string",
+      "description": "变更前的 JSON 值（CREATE 时为空）"
+     },
+     "afterJson": {
+      "type": "string",
+      "description": "变更后的 JSON 值（DELETE 时为空）"
+     },
+     "reason": {
+      "type": "string",
+      "description": "变更原因",
+      "maxLength": 500
+     }
+    },
+    "description": "配置变更提交审批请求 DTO。\n\n前端在配置管理页面发起审批流时提交，包含资源标识、变更前后值和原因。",
+    "x-field-docs": {
+     "resourceType": "资源类型（CONFIG / DICT / VARIABLE）",
+     "resourceKey": "资源唯一标识（如配置键、字典类型编码、变量键）",
+     "resourceGroup": "资源分组（仅 CONFIG 类型有值）",
+     "changeType": "变更操作类型（CREATE / UPDATE / DELETE）",
+     "beforeJson": "变更前的 JSON 值（CREATE 时为空）",
+     "afterJson": "变更后的 JSON 值（DELETE 时为空）",
+     "reason": "变更原因"
+    },
+    "required": [
+     "resourceType",
+     "resourceKey",
+     "changeType"
+    ]
    },
    "ConfigPageQuery": {
     "type": "object",
@@ -2386,10 +3123,12 @@
       "type": "string"
      },
      "configGroup": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 64
      },
      "configKey": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 128
      },
      "configValue": {
       "type": "string"
@@ -2404,10 +3143,9 @@
       "type": "string"
      },
      "isPublic": {
-      "type": "integer",
-      "format": "int32"
+      "type": "boolean"
      },
-     "sortOrder": {
+     "sort": {
       "type": "integer",
       "format": "int32"
      },
@@ -2415,8 +3153,13 @@
       "type": "string"
      }
     },
-    "description": "系统配置创建/更新 DTO\n\n对应 `ydsz_sys_config` 表的写入参数，是「系统配置中心」创建 / 更新接口的入参载体。 创建时 `id` 为空（由雪花算法自动生成），更新时\n`id` 必填。\n\nP1-5 字段重叠处理：本类与 ConfigVO 字段完全一致，遵循《云顶编码规范》\"VO 兼 DTO\"模式（字段无差异时不作三分离）。 作为\nRepository 层的写入契约存在，避免 infra 层直接依赖 ConfigVO（保持分层边界清晰）。\n\n维护约定：新增字段时需同步 ConfigVO，确保两者结构一致。\n\n字段语义：\n\n\n`configGroup` — 配置分组，按业务域分类管理\n`configKey` — 配置键，同组内唯一标识\n`configValue` — 配置值\n`valueType` — 值类型: STRING/NUMBER/BOOLEAN/JSON\n`defaultValue` — 默认值（配置未设置时使用）\n`isPublic` — 是否对前端公开: 1 公开 / 0 仅后端\n`sortOrder` — 排序序号\n`status` — 启用状态: ENABLED/DISABLED",
-    "x-field-docs": {}
+    "description": "系统配置创建/更新 DTO\n\n对应 `ydsz_sys_config` 表的写入参数，是「系统配置中心」创建 / 更新接口的入参载体。 创建时 `id` 为空（由雪花算法自动生成），更新时\n`id` 必填。\n\nP1-5 字段重叠处理：本类与 ConfigVO 字段完全一致，遵循《云顶编码规范》\"VO 兼 DTO\"模式（字段无差异时不作三分离）。 作为\nRepository 层的写入契约存在，避免 infra 层直接依赖 ConfigVO（保持分层边界清晰）。\n\n维护约定：新增字段时需同步 ConfigVO，确保两者结构一致。\n\n字段语义：\n\n\n`configGroup` — 配置分组，按业务域分类管理\n`configKey` — 配置键，同组内唯一标识\n`configValue` — 配置值\n`valueType` — 值类型: STRING/NUMBER/BOOLEAN/JSON\n`defaultValue` — 默认值（配置未设置时使用）\n`public` — 是否对前端公开: true 公开 / false 仅后端\n`sort` — 排序序号\n`status` — 启用状态: ENABLED/DISABLED",
+    "x-field-docs": {},
+    "required": [
+     "configGroup",
+     "configKey",
+     "valueType"
+    ]
    },
    "ConfigBatchDTO": {
     "type": "object",
@@ -2425,11 +3168,15 @@
       "type": "array",
       "items": {
        "$ref": "#/components/schemas/ConfigVO"
-      }
+      },
+      "maxLength": 500
      }
     },
     "description": "系统配置批量操作 DTO\n\n用于批量创建配置项（运营初始化场景），单次最多 500 条。",
-    "x-field-docs": {}
+    "x-field-docs": {},
+    "required": [
+     "items"
+    ]
    },
    "ConfigVO": {
     "type": "object",
@@ -2456,10 +3203,9 @@
       "type": "string"
      },
      "isPublic": {
-      "type": "integer",
-      "format": "int32"
+      "type": "boolean"
      },
-     "sortOrder": {
+     "sort": {
       "type": "integer",
       "format": "int32"
      },
@@ -2467,7 +3213,7 @@
       "type": "string"
      }
     },
-    "description": "系统配置 VO（视图对象）\n\n对应 `ydsz_sys_config` 表的展示视图，是「系统配置中心」列表 / 详情接口的响应载体。\n由 com.njydsz.system.infra.converter.SystemConverter 从 {@link\ncom.njydsz.system.infra.entity.Config} 实体转换而来。\n\n字段语义：\n\n\n`configValue` — 配置值，序列化时按 `valueType` 转换： `STRING` 原样输出；{@code NUMBER /\nBOOLEAN / JSON} 解析为对应类型\n`defaultValue` — 默认值（`configValue` 为空时回退）\n`isPublic` — 是否对前端公开：`1` 公开 / `0` 仅后端； 前端「公开配置」接口仅返回 `isPublic=1`\n的项\n\n\n使用场景：\n\n\n配置中心列表 / 详情 / 编辑回显\n业务参数动态下发（`ydsz.workflow.sla-default-hours` 等）\n前端「公开配置」接口返回值（开关、限流阈值、特性开关等）\n\n\n缓存策略：读取通过 ydsz-common-cache 进程内本地缓存（键 `value:{tenantId`:{configKey}}）；\n写入时 `@CacheEvict` 精准失效；TTL 通过 `ydsz.cache.caches.system:config` 配置（默认 5 分钟）。\n\n注意：本类为视图对象，不包含输入校验逻辑。输入校验由 {@link\ncom.njydsz.system.domain.dto.ConfigDTO} 负责。",
+    "description": "系统配置 VO（视图对象）\n\n对应 `ydsz_sys_config` 表的展示视图，是「系统配置中心」列表 / 详情接口的响应载体。\n由 SystemConverter 从 {@link\nConfig} 实体转换而来。\n\n字段语义：\n\n\n`configValue` — 配置值，序列化时按 `valueType` 转换： `STRING` 原样输出；{@code NUMBER /\nBOOLEAN / JSON} 解析为对应类型\n`defaultValue` — 默认值（`configValue` 为空时回退）\n`isPublic` — 是否对前端公开：`true` 公开 / `false` 仅后端； 前端「公开配置」接口仅返回 `isPublic=true`\n的项\n\n\n使用场景：\n\n\n配置中心列表 / 详情 / 编辑回显\n业务参数动态下发（`ydsz.workflow.sla-default-hours` 等）\n前端「公开配置」接口返回值（开关、限流阈值、特性开关等）\n\n\n缓存策略：读取通过 ydsz-common-cache 进程内本地缓存（键 `value:{tenantId`:{configKey}}）；\n写入时 `@CacheEvict` 精准失效；TTL 通过 `ydsz.cache.caches.system:config` 配置（默认 5 分钟）。\n\n注意：本类为视图对象，不包含输入校验逻辑。输入校验由 {@link\ncom.njydsz.system.domain.dto.ConfigDTO} 负责。",
     "x-field-docs": {}
    },
    "DictPageQuery": {
@@ -2497,10 +3243,12 @@
       "type": "string"
      },
      "typeCode": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 64
      },
      "typeName": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 128
      },
      "description": {
       "type": "string"
@@ -2510,7 +3258,11 @@
      }
     },
     "description": "字典类型 DTO\n\n用于字典类型的创建和更新操作，作为 Repository 接口 CUD 方法的入参。\n\n字段语义：\n\n\n`id` — 主键 ID（更新时必填）\n`typeCode` — 字典类型编码，租户内唯一\n`typeName` — 字典类型名称\n`description` — 字典类型业务说明\n`status` — 启用状态: ENABLED/DISABLED",
-    "x-field-docs": {}
+    "x-field-docs": {},
+    "required": [
+     "typeCode",
+     "typeName"
+    ]
    },
    "DictItemPageQuery": {
     "type": "object",
@@ -2542,15 +3294,18 @@
       "type": "string"
      },
      "typeCode": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 64
      },
      "itemCode": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 64
      },
      "itemValue": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 255
      },
-     "sortOrder": {
+     "sort": {
       "type": "integer",
       "format": "int32"
      },
@@ -2564,8 +3319,13 @@
       "type": "string"
      }
     },
-    "description": "字典项 DTO\n\n用于字典项的创建和更新操作，作为 Repository 接口 CUD 方法的入参。\n\n字段语义：\n\n\n`id` — 主键 ID（更新时必填）\n`typeCode` — 所属字典类型编码\n`itemCode` — 字典项编码\n`itemValue` — 字典项展示值\n`parentId` — 父级 ID\n`sortOrder` — 排序号\n`description` — 字典项业务说明\n`extJson` — 扩展属性 JSON\n`status` — 启用状态: ENABLED/DISABLED",
-    "x-field-docs": {}
+    "description": "字典项 DTO\n\n用于字典项的创建和更新操作，作为 Repository 接口 CUD 方法的入参。\n\n字段语义：\n\n\n`id` — 主键 ID（更新时必填）\n`typeCode` — 所属字典类型编码\n`itemCode` — 字典项编码\n`itemValue` — 字典项展示值\n`parentId` — 父级 ID\n`sort` — 排序号\n`description` — 字典项业务说明\n`extJson` — 扩展属性 JSON\n`status` — 启用状态: ENABLED/DISABLED",
+    "x-field-docs": {},
+    "required": [
+     "typeCode",
+     "itemCode",
+     "itemValue"
+    ]
    },
    "DictItemBatchDTO": {
     "type": "object",
@@ -2574,11 +3334,15 @@
       "type": "array",
       "items": {
        "$ref": "#/components/schemas/DictItemVO"
-      }
+      },
+      "maxLength": 500
      }
     },
     "description": "字典项批量操作 DTO\n\n用于批量新增字典项（运营初始化场景），单次最多 500 条。",
-    "x-field-docs": {}
+    "x-field-docs": {},
+    "required": [
+     "items"
+    ]
    },
    "DictItemVO": {
     "type": "object",
@@ -2616,7 +3380,7 @@
      "itemValue": {
       "type": "string"
      },
-     "sortOrder": {
+     "sort": {
       "type": "integer",
       "format": "int32",
       "description": "排序号（同类型内升序），直接作为 TreeBuilder 排序字段"
@@ -2631,14 +3395,14 @@
       "type": "string"
      }
     },
-    "description": "字典项 VO（视图对象）\n\n对应 `ydsz_sys_dict_item` 表的展示视图，是「字典中心」列表 / 详情接口的响应载体，\n也是前端下拉框、单选框、级联选择器、Tag 组件的核心数据载体。\n\n使用 com.njydsz.common.domain.tree.TreeBuilder#buildSimple 构建树形结构，无需继承 `TreeNode` 基类，\n保持 VO 类的纯粹性（无框架耦合），同时通过 `level`/`path` 回调自动填充层级元数据。\n\n字段语义：\n\n\n`typeCode` — 所属字典类型编码，与 DictTypeVO#typeCode 关联\n`itemCode` — 字典项编码（业务存储值，`<el-option :value=\"...\">`）\n`itemValue` — 字典项展示值（前端展示文本）\n`parentId` — 父级 ID（`0` = 根），支持「省 / 市 / 区县」三级级联\n`sortOrder` — 同类型内排序号（升序），直接作为 TreeBuilder 排序字段\n`extJson` — 扩展属性 JSON（如地区码、颜色值、图标等），前端按需解析\n\n\n使用场景：\n\n\n字典中心列表 / 详情\n前端通用下拉框 / 单选框 / 多选框 / 级联选择 / 树形字典渲染\n业务表单中的「枚举字段」数据源\n\n\n缓存策略：读取时按 `ydsz:dict:item:{typeCode`} 整体缓存至 Redis； 列表查询支持本地缓存（Caffeine），TTL 5min。\n\n注意：本类为视图对象，不包含输入校验逻辑。输入校验由 {@link\ncom.njydsz.system.domain.dto.DictItemDTO} 负责。\n\n构建树形结构，消除业务 VO 与框架基类的耦合",
+    "description": "字典项 VO（视图对象）\n\n对应 `ydsz_sys_dict_item` 表的展示视图，是「字典中心」列表 / 详情接口的响应载体，\n也是前端下拉框、单选框、级联选择器、Tag 组件的核心数据载体。\n\n使用 com.njydsz.common.domain.tree.TreeBuilder#buildSimple 构建树形结构，无需继承 `TreeNode` 基类，\n保持 VO 类的纯粹性（无框架耦合），同时通过 `level`/`path` 回调自动填充层级元数据。\n\n字段语义：\n\n\n`typeCode` — 所属字典类型编码，与 DictTypeVO#typeCode 关联\n`itemCode` — 字典项编码（业务存储值，`<el-option :value=\"...\">`）\n`itemValue` — 字典项展示值（前端展示文本）\n`parentId` — 父级 ID（`0` = 根），支持「省 / 市 / 区县」三级级联\n`sort` — 同类型内排序号（升序），直接作为 TreeBuilder 排序字段\n`extJson` — 扩展属性 JSON（如地区码、颜色值、图标等），前端按需解析\n\n\n使用场景：\n\n\n字典中心列表 / 详情\n前端通用下拉框 / 单选框 / 多选框 / 级联选择 / 树形字典渲染\n业务表单中的「枚举字段」数据源\n\n\n缓存策略：读取时按 `ydsz:dict:item:{typeCode`} 整体缓存至 Redis； 列表查询支持本地缓存（Caffeine），TTL 5min。\n\n注意：本类为视图对象，不包含输入校验逻辑。输入校验由 {@link\ncom.njydsz.system.domain.dto.DictItemDTO} 负责。\n\n构建树形结构，消除业务 VO 与框架基类的耦合",
     "x-field-docs": {
      "id": "字典项唯一标识",
      "parentId": "父级 ID（`0` = 根），支持「省 / 市 / 区县」三级级联",
      "children": "子节点列表（树形构建时由 TreeBuilder 自动填充）",
      "level": "层级深度（根节点=1，由 TreeBuilder 自动填充）",
      "path": "节点路径（如 \"/1/2/5/\"，由 TreeBuilder 自动填充）",
-     "sortOrder": "排序号（同类型内升序），直接作为 TreeBuilder 排序字段"
+     "sort": "排序号（同类型内升序），直接作为 TreeBuilder 排序字段"
     }
    },
    "EntityVersionPageQuery": {
@@ -2688,7 +3452,10 @@
     "description": "配置查询请求 DTO。",
     "x-field-docs": {
      "key": "配置键"
-    }
+    },
+    "required": [
+     "key"
+    ]
    },
    "DictItemGetRequest": {
     "type": "object",
@@ -2706,7 +3473,11 @@
     "x-field-docs": {
      "typeCode": "字典类型编码",
      "itemCode": "字典项编码"
-    }
+    },
+    "required": [
+     "typeCode",
+     "itemCode"
+    ]
    },
    "DictListRequest": {
     "type": "object",
@@ -2719,7 +3490,10 @@
     "description": "字典项列表查询请求 DTO。\n\n用于 InternalApiController 内部 API 的字典项列表查询，通过 POST body 传输参数，\n与同模块其他请求 DTO 保持风格一致（DictItemGetRequest、ConfigGetRequest、\nAppValidateRequest）。",
     "x-field-docs": {
      "typeCode": "字典类型编码"
-    }
+    },
+    "required": [
+     "typeCode"
+    ]
    },
    "AppValidateRequest": {
     "type": "object",
@@ -2737,7 +3511,49 @@
     "x-field-docs": {
      "appKey": "应用 Key（client_id）",
      "appSecret": "应用密钥（client_secret）"
-    }
+    },
+    "required": [
+     "appKey",
+     "appSecret"
+    ]
+   },
+   "SecondaryAuthRequest": {
+    "type": "object",
+    "properties": {
+     "password": {
+      "type": "string",
+      "description": "当前登录用户的明文密码。\n\n用于验证用户身份，验证通过后写入场景化安全标记。"
+     },
+     "scene": {
+      "type": "string",
+      "description": "场景标识（scene）。\n\n用于区分不同业务场景的二级认证，每个场景独立验证、独立过期。\n常用值：`password_change`、`role_assign`、`data_export`、`tenant_config`"
+     },
+     "ttlSeconds": {
+      "type": "integer",
+      "format": "int32",
+      "description": "二级认证有效期（秒）。\n\n默认 300 秒（5 分钟）。CRITICAL 级别会自动缩短为 40%。"
+     },
+     "level": {
+      "type": "string",
+      "enum": [
+       "MEDIUM",
+       "HIGH",
+       "CRITICAL"
+      ],
+      "description": "敏感操作等级。\n\n默认 HIGH。CRITICAL 级别使用更短的验证窗口（TTL 的 40%，最小 60 秒）。"
+     }
+    },
+    "description": "场景化二级认证请求体（P0-2 标准化）。\n\n前端调用 `Post /api/auth/secondary-auth` 接口时传入，包含当前用户密码和目标场景标识。",
+    "x-field-docs": {
+     "password": "当前登录用户的明文密码。\n\n用于验证用户身份，验证通过后写入场景化安全标记。",
+     "scene": "场景标识（scene）。\n\n用于区分不同业务场景的二级认证，每个场景独立验证、独立过期。\n常用值：`password_change`、`role_assign`、`data_export`、`tenant_config`",
+     "ttlSeconds": "二级认证有效期（秒）。\n\n默认 300 秒（5 分钟）。CRITICAL 级别会自动缩短为 40%。",
+     "level": "敏感操作等级。\n\n默认 HIGH。CRITICAL 级别使用更短的验证窗口（TTL 的 40%，最小 60 秒）。"
+    },
+    "required": [
+     "password",
+     "scene"
+    ]
    },
    "TenantPageQuery": {
     "type": "object",
@@ -2766,19 +3582,25 @@
       "type": "string"
      },
      "tenantCode": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 64
      },
      "tenantName": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 128
      },
      "contactName": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 64
      },
      "contactPhone": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 32
      },
      "contactEmail": {
-      "type": "string"
+      "type": "string",
+      "format": "email",
+      "maxLength": 128
      },
      "planId": {
       "type": "string"
@@ -2794,11 +3616,16 @@
       "type": "string"
      },
      "remark": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 512
      }
     },
     "description": "租户创建/更新 DTO\n\n对应 `ydsz_sys_tenant` 表的写入参数。 创建时 `id` 为空（由雪花算法自动生成），更新时 `id` 必填。\n\n字段约束：\n\n\n`tenantCode` — 租户编码，全局唯一，最长 64 字符\n`tenantName` — 租户名称，最长 128 字符",
-    "x-field-docs": {}
+    "x-field-docs": {},
+    "required": [
+     "tenantCode",
+     "tenantName"
+    ]
    },
    "TenantPlanPageQuery": {
     "type": "object",
@@ -2824,30 +3651,39 @@
       "type": "string"
      },
      "planCode": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 64
      },
      "planName": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 128
      },
      "description": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 512
      },
-     "sortOrder": {
+     "sort": {
       "type": "integer",
       "format": "int32"
      },
      "quotaJson": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 2048
      },
      "featureJson": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 2048
      },
      "status": {
       "type": "string"
      }
     },
-    "description": "租户套餐创建/更新 DTO\n\n对应 `ydsz_sys_tenant_plan` 表的写入参数，是「套餐管理」创建 / 更新接口的入参载体。\n创建时 `id` 为空（由雪花算法自动生成），更新时 `id` 必填。\n\n字段语义：\n\n\n`planCode` — 套餐编码，全局唯一标识\n`sortOrder` — 排序号（升序，影响前端套餐选择器顺序）",
-    "x-field-docs": {}
+    "description": "租户套餐创建/更新 DTO\n\n对应 `ydsz_sys_tenant_plan` 表的写入参数，是「套餐管理」创建 / 更新接口的入参载体。\n创建时 `id` 为空（由雪花算法自动生成），更新时 `id` 必填。\n\n字段语义：\n\n\n`planCode` — 套餐编码，全局唯一标识\n`sort` — 排序号（升序，影响前端套餐选择器顺序）",
+    "x-field-docs": {},
+    "required": [
+     "planCode",
+     "planName"
+    ]
    },
    "TenantPlanMenuDTO": {
     "type": "object",
@@ -2863,7 +3699,10 @@
      }
     },
     "description": "租户套餐-菜单关联 DTO\n\n用于为套餐批量配置可访问菜单列表。 通过套餐 ID + 菜单 ID 列表一次性设置套餐的菜单权限。",
-    "x-field-docs": {}
+    "x-field-docs": {},
+    "required": [
+     "planId"
+    ]
    },
    "VariablePageQuery": {
     "type": "object",
@@ -2889,7 +3728,8 @@
       "type": "string"
      },
      "variableKey": {
-      "type": "string"
+      "type": "string",
+      "maxLength": 128
      },
      "variableValue": {
       "type": "string"
@@ -2905,7 +3745,11 @@
      }
     },
     "description": "系统变量创建/更新 DTO\n\n对应 `ydsz_sys_variable` 表的写入参数，是「系统变量中心」创建 / 更新接口的入参载体。\n创建时 `id` 为空（由雪花算法自动生成），更新时 `id` 必填。\n\n字段语义：\n\n\n`variableKey` — 变量键，租户内唯一\n`variableValue` — 变量值\n`valueType` — 值类型: STRING/NUMBER/BOOLEAN/JSON\n`status` — 启用状态: ENABLED/DISABLED",
-    "x-field-docs": {}
+    "x-field-docs": {},
+    "required": [
+     "variableKey",
+     "valueType"
+    ]
    }
   }
  }
diff --git a/apps/system-web/src/api/tenant.ts b/apps/system-web/src/api/tenant.ts
index 04e7e44cc..7be466d89 100644
--- a/apps/system-web/src/api/tenant.ts
+++ b/apps/system-web/src/api/tenant.ts
@@ -3,8 +3,8 @@
 /**
  * TenantController API 封装（auto-generated by bash/gen-contract.py）
  *
- * <p>对应后端 {@code TenantController}，共 5 个端点。
- * <p>路径规范: /api/system/**（kebab-case），成功码统一为 code === 'A00000'。
+ * <p>对应后端 {@code TenantController}，共 6 个端点。
+ * <p>路径规范: /api/v1/system/**（kebab-case），成功码统一为 code === 'A00000'。
  *
  * @author ydsz-team
  * @auto-generated 请勿手动修改；后端契约变更后执行 {@code python bash/gen-contract.py system} 重新生成
@@ -14,6 +14,13 @@ import { requestClient } from '#/api/request';
 import type { PageResponse } from './models';
 import type { TenantDTO, TenantPageQuery, TenantVO } from './models';
 
+/**
+ * listAccessible: GET /api/tenant/accessible
+ */
+export function listAccessible(): Promise<TenantVO[]> {
+  return requestClient.get<TenantVO[]>(`/api/tenant/accessible`);
+}
+
 /**
  * page: GET /api/tenant/page
  */
diff --git a/apps/system-web/src/api/tenantPlan.ts b/apps/system-web/src/api/tenantPlan.ts
index fa5cb407e..7ef74e8a9 100644
--- a/apps/system-web/src/api/tenantPlan.ts
+++ b/apps/system-web/src/api/tenantPlan.ts
@@ -4,7 +4,7 @@
  * TenantPlanController API 封装（auto-generated by bash/gen-contract.py）
  *
  * <p>对应后端 {@code TenantPlanController}，共 8 个端点。
- * <p>路径规范: /api/system/**（kebab-case），成功码统一为 code === 'A00000'。
+ * <p>路径规范: /api/v1/system/**（kebab-case），成功码统一为 code === 'A00000'。
  *
  * @author ydsz-team
  * @auto-generated 请勿手动修改；后端契约变更后执行 {@code python bash/gen-contract.py system} 重新生成
diff --git a/apps/system-web/src/api/variable.ts b/apps/system-web/src/api/variable.ts
index 77f574b28..399370a70 100644
--- a/apps/system-web/src/api/variable.ts
+++ b/apps/system-web/src/api/variable.ts
@@ -4,7 +4,7 @@
  * VariableController API 封装（auto-generated by bash/gen-contract.py）
  *
  * <p>对应后端 {@code VariableController}，共 6 个端点。
- * <p>路径规范: /api/system/**（kebab-case），成功码统一为 code === 'A00000'。
+ * <p>路径规范: /api/v1/system/**（kebab-case），成功码统一为 code === 'A00000'。
  *
  * @author ydsz-team
  * @auto-generated 请勿手动修改；后端契约变更后执行 {@code python bash/gen-contract.py system} 重新生成
diff --git a/apps/system-web/src/api/variableVersion.ts b/apps/system-web/src/api/variableVersion.ts
index a00e46015..17d753b25 100644
--- a/apps/system-web/src/api/variableVersion.ts
+++ b/apps/system-web/src/api/variableVersion.ts
@@ -4,7 +4,7 @@
  * VariableVersionController API 封装（auto-generated by bash/gen-contract.py）
  *
  * <p>对应后端 {@code VariableVersionController}，共 2 个端点。
- * <p>路径规范: /api/system/**（kebab-case），成功码统一为 code === 'A00000'。
+ * <p>路径规范: /api/v1/system/**（kebab-case），成功码统一为 code === 'A00000'。
  *
  * @author ydsz-team
  * @auto-generated 请勿手动修改；后端契约变更后执行 {@code python bash/gen-contract.py system} 重新生成
