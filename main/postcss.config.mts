/**
 * main 应用 PostCSS 配置。
 *
 * @remarks
 * 复用共享包 {@code @ydsz/tailwind-config/postcss} 的默认配置，
 * 保持与各子应用一致的样式处理管线。
 *
 * @author ydsz-team
 * @since 1.0.0
 */
export { default } from '@ydsz/tailwind-config/postcss';
