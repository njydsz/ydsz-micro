/**
 * TenantContext 挂件 — 顶栏租户切换器
 *
 * <p>在 BasicLayout 顶栏右侧展示当前活跃租户，提供下拉切换可访问租户能力。
 * 仅当存在可访问租户列表且有多个租户时显示切换入口。
 *
 * @path comm\effects\layouts\src\widgets\tenant-context\index.ts
 * @author ydsz-team
 * @since 1.0.0
 */
export { default as TenantContext } from './tenant-context.vue';
