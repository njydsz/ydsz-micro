# 安全策略 (Security Policy)

## 支持的版本

| 版本              | 支持状态                      |
| ----------------- | ----------------------------- |
| main 分支（最新） | ✅ 支持                       |
| 历史发布版        | ⚠️ 仅修复高危漏洞（如有发布） |

> 当前项目处于 v1.0.0 快速迭代阶段，建议始终跟进 `main` 分支并依赖最新构建产物。

## 报告安全漏洞

**请勿**在公开 Issue、讨论区或 MR 中提交安全漏洞 —— 这会让漏洞在修复前被公开利用。

请通过以下任一方式**私密**报告：

1. 联系仓库管理员（GitLab 仓库页面 → 成员列表 → 站内私信）
2. 发送邮件至维护者：`limw1888@126.com`（发件主题请以 `[SECURITY]` 开头）

### 报告中请包含

- 受影响模块与版本/提交号
- 漏洞类型与危害评估（CVSS 可选项）
- 完整复现步骤（尽量最小化）
- 如可能，附上修复建议或 PoC

### 处理流程

1. **确认**：48 小时内确认收到报告，并进行初步评估
2. **修复**：按严重程度确定修复优先级，高危漏洞优先处理
3. **披露**：修复合入并发布后，再公开漏洞细节与致谢（如报告者同意）

## 依赖安全

- 仓库在 `pre-push` 阶段自动执行 `pnpm audit`，未修复的高危依赖将阻止推送。
- 建议定期运行 `pnpm update:deps` 并关注上游安全公告。
- 生产依赖统一通过 `pnpm-workspace.yaml` 的 `catalog:` 集中管理版本，便于统一升级。
